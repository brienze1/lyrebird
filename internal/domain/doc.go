// Package domain holds Lyrebird's core entities — Partition, Upstream, Mock,
// Match, Action, Script, Scenario, and TrafficRecord — with zero imports
// outside the standard library, per the constitution's clean-architecture
// requirement (Principle IV).
package domain
