{
  "id": "aws-secrets-manager",
  "title": "AWS Secrets Manager GetSecretValue (JSON 1.1 protocol, X-Amz-Target routing)",
  "provider": "aws",
  "service": "secretsmanager",
  "description": "Secrets Manager also uses an AWS JSON protocol: every operation POSTs to \"/\" with the action named in the X-Amz-Target header (secretsmanager.GetSecretValue), not the URL path. Point the SDK at Lyrebird via AWS_ENDPOINT_URL.",
  "mock": {
    "name": "aws-secrets-manager-get-secret-value",
    "match": {
      "method": "POST",
      "path": "/",
      "headers": { "X-Amz-Target": { "equals": "secretsmanager.GetSecretValue" } }
    },
    "action": {
      "respond": {
        "status": 200,
        "headers": { "Content-Type": "application/x-amz-json-1.1" },
        "body": "{\"Name\":\"example-secret\",\"SecretString\":\"{\\\"username\\\":\\\"demo\\\",\\\"password\\\":\\\"demo-password\\\"}\",\"VersionId\":\"00000000-0000-0000-0000-000000000000\"}"
      }
    }
  }
}
